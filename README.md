# Musician app

A SwiftUI iOS music player app. It loads an album/track list from a remote JSON endpoint, caches it locally, and lets the user browse albums, pick tracks, and play them with a mini audio player.

More info at `CLAUDE.md`.
